# Changelog

## 0.2.0

- `<lingo-switcher>` custom element, also exported as `lingoweave/switcher`. Picks a language, fires `lingo-change`, calls `setLanguage()`.
- Demo page at https://harshit-d3v.github.io/lingoweave/ with an offline `?provider=fake` mode.
- Playwright test in real Chromium runs in CI next to the unit suite.
- `vercel.json` so the repo deploys on Vercel as-is.

## 0.1.2

- Docs and comments rewritten, npm description fixed.
- Repository, homepage and bugs fields in package.json.
- Dev dependencies updated (vitest 4, happy-dom 20, size-limit 14).
- CI workflow.

## 0.1.1

- First public release.
